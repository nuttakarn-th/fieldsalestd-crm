@echo off
cd /d "%~dp0"
if exist .git\HEAD.lock del /f .git\HEAD.lock
if exist .git\index.lock del /f .git\index.lock
git add -A
git status
git commit -m "feat: Dashboard — Vehicle Summary replaced with Calendar Grid View (VehicleCalendar component)"
git push
pause
